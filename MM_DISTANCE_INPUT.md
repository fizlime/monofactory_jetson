# mm 거리 입력

P01/P04의 거리 설정과 수동 이동은 사용자가 입력한 mm 값을 사용한다.
환산 안내는 화면에 표시하지 않는다. 시간·속도·통신 설정은 각 단위를 유지하고,
Dobot J1~J4는 관절각(°)을 유지한다.

- MANUAL P01: 전진/후진 거리를 mm로 입력하고 해당 버튼 실행.
- SETTING P01: 자동 왕복의 편도 거리, HOME 최대 탐색 및 센서 확인 거리를 mm로 입력.
- MANUAL P04: 하강/상승 거리를 mm로 입력하고 해당 버튼 실행.
- SETTING P03·P04·P05: 통합 순서의 자동 하강/상승 거리를 mm로 입력하고 설정 저장.
  모든 하강 동작은 공통 하강 거리, 모든 상승 동작은 공통 상승 거리를 사용한다.
- 프레스 현재 위치·저장 위치·감속 선행 거리도 mm 단위.
- 수동으로 입력한 거리는 해당 수동 명령에만 적용되며 자동 설정을 덮어쓰지 않는다.

HTTP 수동 명령 예시:

```json
{"action":"FORWARD","distance_mm":12.5}
```

```json
{"action":"DOWN","distance_mm":2.25}
```

설정 필드:

- `p01.axes.E0.distance_mm` (E0~E3)
- `p01.axes.E0.home_search_mm`, `home_step_mm`
- `p04.down_mm`, `p04.up_mm`, `p04.saved_positions_mm`
- `p04.can.deceleration_advance_mm`

소수점 입력을 지원한다. 장치에 보낼 때만 정수 펄스/스텝으로 반올림하며,
0으로 반올림되는 너무 작은 이동, 음수 수동 거리, 비정상 숫자, 단위 혼합은 거부한다.
기존 펄스/스텝 설정은 같은 명령량의 mm로 한 번 마이그레이션한다.
기존 명시적 `pulses`/`steps` API는 호환되지만 신규 화면에서는 사용하지 않는다.
상태 API에는 `position_mm`를 추가했으며 기존 원시 `position` 필드는 호환용으로 유지한다.

모터 사용 여부, 자재 감지, ENABLE/HOME/STOP 조건, CAN/UART 통신 방식,
27개 통합 동작 순서와 Dobot 공유 설정은 유지한다.
배포 검증은 가상 장치로 수행하며 실제 이동·자동 사이클은 실행하지 않는다.
