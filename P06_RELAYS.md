# P06 Uno 릴레이 제어

| 출력 | Uno 핀 | 수동 명령 |
|---|---|---|
| 초록 경광등 | D8 | ON / CHANNEL_OFF |
| 빨강 경광등 | D9 | ON / CHANNEL_OFF |
| 부저 | D10 | ON / CHANNEL_OFF |

나머지 릴레이 2개는 예비이며 코드는 핀을 할당하거나 조작하지 않습니다. 기존 센서 D2~D5를 유지합니다.

MANUAL P06에서 각 출력을 독립적으로 켜고 끕니다. ALL OFF는 세 출력을 함께 끕니다. 기존 RED/GREEN 명령은 자동 결과 표시용으로 색상을 단독 점등하고 부저를 끕니다. 부저 자동 경보 패턴은 추가하지 않았습니다. 기존 MES 동작은 유지합니다.

SETTING P06의 `relay_active_level`은 UNSET(초기값), LOW, HIGH입니다. 극성을 모르면 UNSET으로 두며 실제 ON 명령은 전송하지 않습니다. P06 장치는 REAL 모드에서 시뮬레이션 성공을 표시하지 않습니다. 펌웨어가 없거나 응답을 받지 못하면 미연결/확인 실패를 표시합니다.

Uno USB 포트는 기존 P00 센서 입력 객체 하나가 소유합니다. 115200 baud의 센서 문자열과 `P06 READY/ACK/STATE` 줄을 구분하며 별도 포트나 임의 GPIO 입력을 사용하지 않습니다. P06 명령이 처리되는 동안에도 센서 프레임을 반영합니다. 명령 번호·극성·3비트 출력값이 모두 맞는 ACK만 성공으로 처리합니다.

펌웨어는 3초 동안 유지 신호를 받지 못하면 OFF로 바뀝니다. 연결 복구만으로 이전 ON 명령을 자동 재실행하지 않습니다. 출력 상태는 Uno가 보고한 GPIO 논리 상태이며 접점의 실제 피드백은 아닙니다.

## 펌웨어 적용 전 준비

`firmware/p06_relays/README.md`에 기존 센서 스케치에 추가할 방법이 있습니다. 모듈의 LOW/HIGH 동작을 확인한 다음 펌웨어의 `P06_RELAY_ON_LEVEL`과 웹 설정을 일치시켜야 합니다. 현재 극성이 미확인이고 기존 Uno 센서 스케치 소스를 확보하지 못했으므로 보드 업로드를 수행하지 않았습니다.

헤더 단독 Uno 래퍼는 LOW/HIGH 모두 Arduino AVR Uno 대상으로 컴파일했습니다. 기존 센서 스케치와 합친 최종 스케치는 소스 확보 후 추가 컴파일·업로드가 필요합니다.
