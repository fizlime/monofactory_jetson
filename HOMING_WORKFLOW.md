# 명시적 호밍과 사이클 실행

MAIN의 HOMING 버튼은 P01 → P03 → P04 → P05 순서로 호밍 작업을 시작합니다.
P03과 P05가 하나의 Dobot을 공유하면 물리적 HOME 명령은 한 번만 보냅니다.
별도 장치로 설정되어 있으면 각각 호밍합니다. 호밍 중에는 START와 수동 명령이 잠기며 STOP은 사용할 수 있습니다.

MANUAL의 P01/P03/P04/P05에는 해당 공정 HOMING 버튼이 있습니다.
기존 축별 HOME 버튼도 유지합니다. 카메라, 자재 감지, 경광등은 호밍 대상이 아닙니다.

P01 원점 센서는 EE-SX 설치 예정입니다. 실제 입력이 연결되지 않은 축은
‘EE-SX 원점 센서 설치 대기’로 남고, 이동이나 가짜 원점 완료 처리를 하지 않습니다.
현재의 입력 거리 전진·복귀 사이클은 계속 사용할 수 있습니다. E3 임시 제외 설정도 유지합니다.
EE-SX 설치 후에는 모델·출력 방식과 연결 핀을 확인하여 HomeSensor 입력을 연결해야 합니다.
Uno D2~D5 자재 감지 입력을 원점 센서로 사용하지 않습니다.

START 또는 통합 공정 실행 전에는 P03/P04/P05 호밍을 완료해야 합니다.
이후 사이클에서는 호밍하지 않고 저장된 투입 → 프레스 → 배출 순서를 실행합니다.
과거 레시피에 남아 있는 DOBOT_HOME 블록은 사이클에서 생략하며, 새 블록 메뉴에는 제공하지 않습니다.
저장 Joint 위치 이름인 HOME으로 이동하는 동작은 일반 위치 이동이므로 그대로 실행합니다.
Joint 위치 티칭 및 기존 자동 자재 감지 조건은 계속 적용됩니다.

서버 재시작, 드라이버 재연결 또는 원점 유효성이 사라진 경우에는 다시 HOMING이 필요합니다.
호밍 실패 시 다음 장비로 진행하지 않으며, 기존 알람 확인·리셋 절차를 사용합니다.
STOP으로 중단된 호밍은 완료 처리하지 않습니다.

API: POST /api/line/home, POST /api/process/P01/home (P03/P04/P05도 동일).
요청은 작업 시작 결과를 즉시 반환하며 완료 상태는 기존 상태 조회/SSE로 확인합니다.

검증은 시뮬레이션 및 가짜 장치/API로 수행합니다. 실제 호밍이나 생산 사이클을 자동 실행하지 않습니다.
