# P01 UART와 수동 이동

MKS SERVO42C V1.1.2 기준: `/dev/ttyTHS1`, 38400 baud, 8N1.

## 반영 사항

- 기본 수신은 정상 동작한 v7과 같은 pyserial raw 8N1입니다. PARMRK는 명시적인 진단 옵션에서만 사용합니다. UART 하드웨어 프레이밍 오류는 수신 데이터를 바꾸지 않는 Linux 카운터로 표시합니다.
- 요청한 길이의 응답만 파싱하고, 첫 조회 응답을 이후의 우연한 체크섬 일치로 덮어쓰지 않습니다.
- 명령 응답을 기다리는 구간을 직렬화합니다. STOP은 기다리기 전에 즉시 전송하며 진행 중 진단을 취소합니다.
- ENABLE/DISABLE/STOP은 응답 누락 시 최대 한 번 재전송합니다. 상대 이동(FD)은 자동 재전송하지 않습니다.
- 짧은 이동에서 시작 응답(01)이 누락됐어도 완료 응답(02)을 받으면 완료로 처리합니다. 시작·완료를 모두 확인하지 못하면 실패로 표시하고 STOP을 보냅니다.
- 입력 pulse와 속도로 필요한 이동 시간을 계산해 기존의 고정 6초 대기만으로 긴 이동이 잘리는 문제를 방지합니다.
- 연결 확인은 EN(0x3A)을 반복 조회합니다. 긴 각도 응답 누락 때문에 정상 모터를 미연결로 처리하지 않습니다. EN을 확인한 결과로 화면의 활성 상태를 갱신하며, 진단 명령으로 모터를 활성화하지 않습니다.
- MANUAL P01은 SIM 모드, 실제 UART 프레임 오류율, 자동 투입의 P00 4/4 감지 필요, 수동 이동의 ENABLE 필요를 구분해 표시합니다.

## 운용 조건

REAL 모드에서 `연결 · EN 상태 확인`을 누릅니다. EN이 비활성이면 ENABLE이 필요합니다. MANUAL 전진·후진·왕복 시험은 센서 감지 없이 가능합니다. 공정 순서의 자동 투입과 P01 공정 단독 실행은 P00 센서 4개 감지를 확인합니다. 기존 E3 제외 설정 및 자동 레시피는 유지합니다.

2026-09-13 진단에서는 명령을 보내지 않는 동안에도 UART 하드웨어 프레이밍 오류가 약 393회/초 증가했습니다. 이 변경은 손상된 수신의 오판과 일부 응답 누락을 처리하지만, 배선·전압·접점에서 생기는 물리 오류를 제거하지는 않습니다. 안정된 실제 운전은 물리 신호 상태를 함께 확인해야 합니다.

공식 문서: https://github.com/makerbase-mks/MKS-SERVO42C/blob/MKS-SERVO42C-V1.1/User%20Manual/MKS%20SERVO42C%20User%20Manual%20V1.1.2.pdf (29쪽: FD 시작/완료 응답)

## v7 명령 호환

`servo42c_protocol.py`에서 정상 동작한 `MONO_POC_P01-P07_CAN_Press_v7`과 동일한 ENABLE(F3), 상대 이동(FD), 방향/속도, 32비트 pulse와 체크섬을 생성합니다. E0~E3의 전진/후진 800 pulse 실제 송신 바이트를 회귀 테스트합니다. 자재 감지 조건은 자동 투입에서 확인하며 수동 이동에는 적용하지 않습니다.
