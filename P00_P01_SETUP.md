# P00 자재 감지 / P01 3축 운용

P00 → P01 → P02 → P03·P04·P05 → P06 순서입니다. P00 감지 대기 후 P01 진입 시 다시 감지를 확인합니다. MANUAL 전진·후진·왕복 시험은 감지와 무관하게 사용할 수 있습니다. P01 공정 단독 실행은 자동 투입과 동일한 감지 조건을 확인합니다. 감지가 확인된 한 투입 묶음은 자재가 센서에서 벗어나도 복귀까지 마칩니다. 입력 미설정·GPIO 오류는 감지로 간주하지 않습니다.

P01 기본 운용 대상은 E0/E1/E2, E3는 임시 제외입니다. SETTING P01의 각 축 **축 사용** 항목에서 변경합니다. 제외한 축을 삭제하거나 주소를 다시 매기지 않으며, 네 번째 모터 추가 후 E3를 사용으로 되돌리면 됩니다. 사용 축의 응답 실패는 무시하지 않습니다. 로봇·프레스의 기존 통합 레시피는 자동으로 바꾸지 않습니다.

## Arduino Uno 센서 입력

E-MSMLS61N-2M 센서 4개를 Arduino Uno에서 읽습니다. D2→자재 1, D3→자재 2, D4→자재 3, D5→자재 4로 대응합니다. **모터 제외 설정과 관계없이 센서 4개가 모두 감지되어야 P00을 통과하고 P01 공정 투입을 시작합니다. 수동 이동에는 이 조건을 적용하지 않습니다.**

- Uno 기존 펌웨어 유지. 115200 baud, 1초 주기로 아래 문자열을 수신합니다.
- `센서 1: 감지 | 센서 2: 미감지 | 센서 3: 감지 | 센서 4: 미감지`
- 시작 안내 `센서 4개 확인 시작`은 감지 데이터로 취급하지 않습니다.
- USB 경로: `/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0`. `/dev/ttyUSB0` 번호가 달라져도 이 경로를 사용합니다. 동일 CH340 보드가 여러 대 연결되면 고유 포트 경로를 다시 지정해야 합니다.
- MANUAL P00에서 각 자재·핀의 상태와 감지 수를 표시합니다. P01에도 `자재 N/4 감지 · 자동 투입 허용/대기`을 표시합니다.
- 각 채널은 감지 상태가 안정 시간(기본 100ms) 이상 유지된 후 받은 다음 프레임에서 감지로 확정합니다. 현재 펌웨어 주기가 1초이므로 감지 확정에 약 1초가 추가로 필요합니다. 미감지는 다음 수신 프레임에서 즉시 반영합니다.
- 유효한 4채널 프레임이 3초 이상 없거나 USB 오류·잘못된 응답이 발생하면 감지 값을 지우고 투입을 차단합니다. 재연결 후 새 프레임으로 확인합니다.
- 감지/미감지 극성은 기존 Uno 프로그램이 판정합니다. Jetson GPIO 핀 및 LOW/HIGH 설정은 이 연결 방식에서 사용하지 않습니다.
- P00 감지 통과 후 P01 시작 시 다시 확인합니다. 투입을 허가받아 시작한 한 묶음은 자재가 이동해 센서에서 벗어나도 복귀까지 마칩니다. 이 입력은 비상정지 장치가 아닙니다.

CH340 드라이버는 Linux v6.8.12의 공식 `drivers/usb/serial/ch341.c`를 현재 Jetson 커널 헤더로 빌드했습니다. 설치 위치는 `/lib/modules/6.8.12-1021-tegra/extra/ch341.ko`, 소스는 `/home/jetson/.local/share/monofactory/ch341-6.8.12`입니다. 커널 버전을 바꾸면 새 커널에 맞는 모듈이 필요합니다.

## P01 연결 진단

MANUAL P01의 **연결 · EN 상태 확인**은 각 사용 축의 EN 상태(0x3A)가 두 번 일치하는지 확인합니다. 각도 조회는 연결 판정의 전제 조건이 아닙니다. ENABLE, 이동, 보정 명령은 보내지 않습니다. 포트 열림과 모터 응답을 구분하며, 체크섬만 맞는 임의의 수신 데이터나 요청 없는 ACK로 연결 성공을 표시하지 않습니다.

2026-09-13 원격 확인:

- /dev/ttyTHS1 @ 38400, 8N1. 포트 점유는 monofactory 하나였고 nvgetty/serial-getty 충돌은 없었습니다.
- 이전 로그에는 E0의 비정상 상태 바이트가 반복됐으며 기존 파서는 이를 연결됨으로 표시했습니다. 해당 수신 처리와 실패 시 enabled 표시를 수정했습니다.
- 서버를 잠시 종료한 독립 조회에서 E0~E3, 9600/19200/38400/57600/115200 모두 수신 0바이트였습니다. 원시 진단 결과는 Jetson `/tmp/monofactory-p01-uart-diagnosis.json`에 보관합니다.
- 이 결과만으로 단선/전원/주소·모드 불일치 중 하나를 확정할 수 없습니다. 세 보드의 전원, 모델, UartAddr(E0/E1/E2), UartBaud(38400), CtrMode(CR_UART), TX/RX/GND 및 다중 UART 배선을 현장에서 확인해야 합니다. 고장 난 보드를 논리적으로 제외해도 물리 버스에 계속 연결되어 신호를 끌어내리면 통신은 복구되지 않습니다.

공식 자료:

- [Makerbase SERVO42C 통신 프로토콜](https://github.com/makerbase-mks/MKS-SERVO42C/wiki/Serial-communication-description)
- [MISUMI 센서 사양·입출력 회로](https://jp.misumi-ec.com/vona2/detail/110310606419/)
- [NVIDIA Jetson GPIO 핀 매핑](https://github.com/NVIDIA/jetson-gpio/blob/master/lib/python/Jetson/GPIO/gpio_pin_data.py)

## USB 재연결과 수신 복구

USB 열림과 유효한 센서 데이터 수신을 구분합니다. MANUAL P00에 포트, baud, 수신 횟수, 형식 오류 횟수, 마지막 정상 수신 시각을 표시합니다. 한 줄의 형식/UTF-8 오류는 즉시 감지 허가를 지우되 포트를 닫아 Uno를 반복 리셋하지 않고 다음 완전한 프레임으로 복구합니다. USB 단절이나 데이터 시간 초과 때는 포트를 다시 연결합니다. 부팅 안내를 다시 받으면 이전 감지값과 안정화 시간을 초기화합니다.
